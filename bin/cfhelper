#!/usr/bin/env python
"""Small helper to invoke the script easily."""

import sys
import os


HERE = os.path.dirname(os.path.realpath(__file__))
sys.path.insert(0, f'{HERE}/..')

from cloudformation_helper.cli import run
run()
